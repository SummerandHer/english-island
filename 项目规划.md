# ISLAND（四六级岛）项目规划

> 面向大学生的垂直四六级学习 Web 平台。一人 + 双 Agent 协作开发，**MVP 目标：3 天内上线可用版本**。

## 协作分工

| Agent | 职责 |
|-------|------|
| 后端资深架构 Agent | 架构设计、库表设计、需求分析、后端编码、API 文档 |
| 资深前端 Agent | UI/UX 实现、性能优化、响应式适配（Web + 平板/iPad） |

**共同约束**：编码前必须阅读 `Agent须知/` 目录下全部文件；前端 Agent 每次提交前再次核对 UI 规范与参考图。

---

## 目的

1. 提供四六级学习交流、阅读翻译能力提升、双语视频学习的综合平台。
2. 建立用户留存，后期通过 VIP 知识付费变现。

**品牌**：ISLAND（四六级岛）  
**视觉风格**：清新校园风（见 `Agent须知/双语模块页面设计参考图.png`）

---

## 目标用户与范围

| 项 | 结论 |
|----|------|
| 用户群 | 中国大陆大学生，备考四六级 |
| 终端 | Web 为主，兼容平板 / iPad（响应式，不做原生 App） |
| 垂直领域 | 只做英语、四六级 |

---

## 四大模块

### 1. 首页模块

- 顶栏导航参考 B 站：「首页」「全平台」
- 首页：学习入口、推荐内容、模块快捷入口
- 全平台：社区 Feed（见模块 4）

### 2. 阅读模块

- 形态：**技巧学习书籍**（网站章节式呈现，非纯刷题 App）
- 内容：阅读方法论、题型技巧、练习引导
- MVP：静态/半静态章节 + 少量**自编模拟题**（标注「模拟题」）

### 3. 翻译模块

- 形态：同阅读模块，章节式技巧内容
- MVP：章节内容 + 自编模拟翻译题
- **VIP 能力**：AI 翻译批改（第一期 AI 功能，见下文）

### 4. 双语视频模块

- UI **必须对齐** `Agent须知/双语模块页面设计参考图.png`
- 布局：左侧上为视频区，其下为播放控制栏，再下为当前句大字幕框；右侧为全文逐句列表（可滚动、高亮当前句）
- 控制栏（MVP 必做）：倍速、全屏、上一句、暂停、下一句、收藏视频
- 控制栏（二期）：隐藏视频、音标、A 点、单句循环、间隔、单句暂停等（参考图已有，MVP 可占位）
- 顶栏模式 Tab（二期）：双语 / 英语 / 中文 / 听写 / 挖空等，MVP 先实现「双语」模式

### 5. 社区（全平台）

- 形态：Feed 流
- UGC 限制：**仅文字 + 图片**，不支持视频/外链帖
- 用户可发帖、浏览、点赞（MVP 评论可选）

---

## VIP 商业化（第二期接入支付，MVP 预留字段与权限）

| VIP 权益 | 说明 |
|----------|------|
| 高级技巧 | 阅读/翻译进阶章节仅 VIP 可读 |
| AI 批改 | 翻译题 AI 评分与改进建议（第一期 AI 场景） |
| 专属视频 | VIP 专享视频列表（与免费 Embed 视频区分） |

---

## 技术选型

### 后端

- **Spring Boot 3.x + JDK 21**，单体架构起步
- 持久层：**MyBatis-Plus 3.5**（Flyway 管表结构）
- 缓存：**Redis**（热点章节、视频元数据、Feed 分页；不缓存侵权视频文件）
- 数据库：**MySQL 8**
- 文件：MVP 存本地 `uploads/`，后续迁移腾讯云 COS / 阿里云 OSS
- 鉴权：JWT + 微信扫码登录（UI 参考 `Agent须知/image.png`）
- 后期规划：Spring Cloud Alibaba、更多 AI Agent 能力

### 前端（已确定）

- **Nuxt 3 + Vue 3 + TypeScript**
- 选型理由（相较 React/Next.js）：
  - 更小的基线包体，符合「轻量化」目标
  - 约定优于配置，3 天 MVP 交付更快
  - SSR/SSG 适合技巧章节 SEO 与首屏加载
  - 与 Spring Boot REST API 配合成熟
- UI：Tailwind CSS + Naive UI（清新校园风易定制）
- 状态：Pinia
- 视频：MVP 用官方 Embed / 外链播放；字幕与逐句同步自研（见 Agent须知）
- 部署：Nitro 构建，可部署至 Node 或静态 CDN

### AI（MVP）

- 场景：**翻译批改**（用户提交译文 → 后端调用 LLM API → 返回评分、错误点、参考表达）
- 接口预留，MVP 可先对接一家国内 LLM（通义/DeepSeek/智谱等，按 API Key 配置）

---

## 架构设计

```
┌─────────────┐     REST/JSON      ┌──────────────────┐
│  Nuxt 3 Web │ ◄────────────────► │ Spring Boot 单体  │
└─────────────┘                    │  ├─ 用户/社区     │
                                   │  ├─ 章节/题库     │
                                   │  ├─ 视频元数据    │
                                   │  ├─ AI 批改       │
                                   │  └─ 文件上传      │
                                   └────────┬─────────┘
                                            │
                              ┌─────────────┼─────────────┐
                              ▼             ▼             ▼
                           MySQL         Redis      uploads/ (→ OSS)
```

- 单体足够支撑 MVP；日活上万后再评估拆分
- **视频不做爬取转存**；MVP 用 Embed + 元数据入库，架构预留 `storage_type` 字段支持后期 4K 自建库

---

## 数据来源与合规

### 题目（已确定）

- **MVP 以自编模拟题为主**，标注「模拟题」，避免真题版权风险
- 入库方式：管理后台手工录入 → 结构化存储（篇章、题干、选项、答案、解析）
- 半自动（后期）：PDF/Word + 人工校对；LLM 辅助抽取需人工审核
- ❌ 不做：批量爬取真题、无授权商用真题

### 视频（已确定）

- **MVP**：官方 Embed（B 站/YouTube 等）+ 站内维护字幕时间轴与双语文稿；不下载、不转存视频文件
- **后期 4K 库扩展**：采购正版素材 / 创作者授权 / 自建上传（`storage_type=local|oss` + CDN），表结构与播放器抽象层 MVP 即预留
- ❌ 不做：爬 YouTube 下载 4K、无授权转载演讲视频
- ⚠️ 大陆用户访问 YouTube 受限，MVP 视频源优先 **B 站 Embed** 或自有 OSS

---

## 库表设计（概要，详细 DDL 由后端 Agent 输出）

| 域 | 核心表 | 备注 |
|----|--------|------|
| 用户 | `user`, `user_profile` | 含 `vip_level`, `vip_expire_at` |
| 订单 | `vip_order` | 第二期 |
| 社区 | `post`, `post_image`, `post_like` | 仅文字+图 |
| 阅读 | `reading_chapter`, `reading_passage`, `reading_question` | 章节+模拟题 |
| 翻译 | `translation_chapter`, `translation_question`, `translation_submission` | 含 AI 批改结果 |
| 词汇 | `vocabulary`, `user_vocabulary` | 生词本 |
| 视频 | `video`, `video_sentence`, `video_favorite`, `user_video_progress` | 含 `storage_type`, `source_url`, `embed_code` |
| 字幕 | `video_sentence` | 句级时间轴，en/zh |
| 合规 | `content_source` | 来源与授权记录 |
| 系统 | `admin_audit_log` | 后台操作 |

---

## 3 天 MVP 范围（必须砍 scope 才能交付）

### ✅ MVP 必做

- [ ] 项目脚手架（前后端、Docker Compose 可选）
- [ ] 微信扫码登录（或 MVP 简化为手机号/邮箱 + 密码，若微信来不及）
- [ ] 首页顶栏 + 四大模块路由
- [ ] 阅读/翻译：各 ≥2 章技巧内容（Markdown/HTML）
- [ ] 翻译：1 道模拟题 + **AI 批改接口**（可接一条 LLM）
- [ ] 双语视频：列表页 + **1 条示例视频**完整播放器（逐句同步、倍速/全屏/上下句/收藏）
- [ ] 全平台：发帖（文字+图）、Feed 列表
- [ ] VIP 字段与权限拦截骨架（可先手动改库测 VIP）
- [ ] `Agent须知` 规范落地

### ⏳ 第二期

- VIP 支付、评论、词汇本、阅读模拟题刷题、视频模式 Tab 全家桶、4K OSS 库、Spring Cloud

---

## 文件与目录约定

```
english/
├── 项目规划.md          # 本文件
├── Agent须知/           # 双 Agent 必读
├── backend/             # Spring Boot
├── frontend/            # Nuxt 3
└── uploads/             # 本地文件（图片等，后期迁 OSS）
```

---

## 注意事项

1. 前端 Agent **每次编码前**阅读 `Agent须知/` 全部文件。
2. 双语模块 UI 以 `双语模块页面设计参考图.png` 为验收标准。
3. 登录页风格参考 `image.png`（微信扫码）。
4. 所有 Agent 产出需考虑大陆网络环境与合规边界。
